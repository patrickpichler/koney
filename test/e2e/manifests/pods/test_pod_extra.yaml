apiVersion: v1
kind: Pod
metadata:
  name: koney-extra-test-pod
  namespace: koney-tests
  labels:
    demo.koney/honeytoken: "true"
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
