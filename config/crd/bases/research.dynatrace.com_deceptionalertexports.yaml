---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: deceptionalertexports.research.dynatrace.com
spec:
  group: research.dynatrace.com
  names:
    kind: DeceptionAlertExport
    listKind: DeceptionAlertExportList
    plural: deceptionalertexports
    singular: deceptionalertexport
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeceptionAlertExport is the Schema for the deceptionalertexports
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the specification of the DeceptionAlertExportSpec.
            properties:
              dynatrace:
                description: Dynatrace describes export rules to Dynatrace
                properties:
                  secretName:
                    description: SecretName references the name of a secret holding
                      `apiToken` and `apiUrl` to connect to the Dynatrace environment.
                    type: string
                  severity:
                    default: HIGH
                    description: Severity describes the severity level upong ingest
                      to Dynatrace.
                    enum:
                    - CRITICAL
                    - HIGH
                    - MEDIUM
                    - LOW
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
